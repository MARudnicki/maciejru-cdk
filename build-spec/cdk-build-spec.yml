version: 0.2

phases:
    install:
      commands:
        - npm install -g npm
        - npm install
    build:
      commans:
        - npm run build
        - npm run cdk -- synth
        - find .

artifacts:
  base-directory: cdk.out/
  files:
    - '**/*'

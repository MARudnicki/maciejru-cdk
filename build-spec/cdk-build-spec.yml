version: 0.2

phases:
  install:
    commands:
      - npm install -g npm
      - npm install
      - echo "test install"
      - find .
  build:
    commands:
      - npm run build
      - npm run test
      - npm run cdk -- synth
      - echo "test build"
      - find .

artifacts:
  base-directory: cdk.out
  files:
    - '**/*'
